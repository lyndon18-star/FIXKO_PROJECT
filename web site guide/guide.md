Fixko — School Facility Reporting & Management System

Landing Page

Deep navy gradient hero with stats (active tickets, resolved this month, avg response time)
Role-selector (Student / Faculty ) with login/signup toggle
School email + ID validation fields, password, and forgot-password link
Student Dashboard (student role)

Sidebar nav: Dashboard, New Report, My Tickets, Notifications
Overview with personal ticket stats and a quick "New Report" CTA
Full report form with: Student ID/email pre-fill (editable for re-verification), building → room selector, category, description, photo upload, priority radio buttons
Ticket list with status + priority badges
Notifications feed
Faculty/Teacher Dashboard (teacher role)

Same structure but scoped to department tickets — shows reporter identity on each ticket
Green color accent distinguishing it from the student portal
Filter tickets by status
Admin Dashboard (admin role)

Overview: critical ticket alert, KPI cards, recent tickets, department breakdown bar chart, inventory alerts
All Tickets: filter by status + priority, click-to-open side panel with status/technician assignment controls
Analytics: most reported items, 6-month open vs resolved bar chart, priority breakdown, avg resolution time by department
Inventory: filterable equipment table with condition badges
Users: role-filtered user table (student/teacher/admin)
Rooms & Buildings: card grid with open ticket counts and QR code buttons
Notifications: admin alerts feed with unread indicators